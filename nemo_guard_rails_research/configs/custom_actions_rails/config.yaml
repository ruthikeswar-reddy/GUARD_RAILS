models:
  - type: main
    engine: openai
    model: gpt-4o-mini

instructions:
  - type: general
    content: |
      You are a helpful enterprise assistant that can check company policies
      and validate content before responding.

rails:
  input:
    flows:
      - check content with custom action

  output:
    flows:
      - validate output with custom action
